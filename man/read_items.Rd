% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_items.R
\name{read_items}
\alias{read_items}
\title{formr-Itemtabelle einlesen -- nur die Struktur}
\usage{
read_items(path)
}
\arguments{
\item{path}{Pfad zur formr-Tabelle (\code{.xlsx} oder \code{.csv}).}
}
\value{
\code{data.frame} mit einer Zeile pro Item: \code{item}, \code{scale},
\code{n_options}, \code{reversed} (offizielle formr-R-Suffix-Konvention),
\code{rev_marker} (projektspezifische \link{rev}-Kennzeichnung). Attribut
\code{source_file} fuer Provenienz.
}
\description{
Liest eine formr-Fragebogentabelle (.xlsx oder .csv) und extrahiert
\strong{ausschliesslich die Struktur}: Itemname, Skalenzuordnung,
Antwortformat, Umkehr-Marker. Die Item-\emph{Texte} (\code{label}) werden
bewusst \strong{nicht} uebernommen -- Itemtexte sind oft urheberrechtlich
geschuetzt; die Struktur ist es nicht. So koennen Struktur-Dateien
bedenkenlos geteilt werden, waehrend Inhalts-Dateien privat bleiben
(Lizenz-Disziplin als Code, nicht als Policy).
}
\details{
Konventionen (aus den realen formr-Tabellen des Projekts):
\itemize{
\item Items = Zeilen, deren \code{type} mit \code{mc} beginnt (ausser
\code{mc_heading}); \code{note}/\code{submit}-Zeilen werden uebersprungen.
\item \verb{[rev]}-Marker in \code{explanations} werden als \code{rev_marker}
uebernommen. \strong{Achtung, Datei-Konvention:} die Bedeutung ist
projektspezifisch (in den BCE-Tabellen dieses Projekts =
\emph{revidierte Fassung}, nicht Umkehr-Kodierung!).
\item Skala = Namens-Praefix ohne Item-Nummer (\code{ctq_3} -> \code{ctq},
\code{BCE_01} -> \code{bce}); ein \code{R}-Suffix nach der Nummer wird dabei
ebenfalls entfernt.
\item \strong{Offizielle formr-Umkehr-Konvention:} Items, deren Name nach
der Nummer auf \code{R} endet (\code{bfi_2R}), werden bei der formr-
Skalenaggregation automatisch umgepolt -> Spalte \code{reversed}.
\item Antwortoptionen, in dieser Reihenfolge gesucht: eigenes
\code{choices}-Blatt der Datei (formr-Standard: Spalte \code{list_name},
eine Zeile pro Option) -> \verb{choice*}-Spalten der Zeile bzw. der
zugehoerigen \code{mc_heading}-Zeile -> bekannte Kuerzel
(\code{ja_nein} = 2) -> sonst \code{NA} mit Warnung.
}
}
