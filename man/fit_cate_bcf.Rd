% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_bcf.R
\name{fit_cate_bcf}
\alias{fit_cate_bcf}
\title{Estimate personalized effects (Bayesian Causal Forest)}
\usage{
fit_cate_bcf(
  stream,
  nburn = 500,
  nsim = 500,
  n_chains = 1L,
  pihat = c("constant", "glm"),
  seed
)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{nburn, nsim}{MCMC burn-in and draws (defaults 500/500).}

\item{n_chains}{Number of MCMC chains (default 1 -- one suffices
for Monte Carlo studies; the bcf default would be 4 parallel
chains = 4x the cost).}

\item{pihat}{Propensity estimate: \code{"constant"} (default --
\code{mean(z)} for everyone; correct under randomization,
\strong{powerless under confounding}) or \code{"glm"} (logistic
regression \code{z ~ x} -- activates bcf's guard against targeted
selection).}

\item{seed}{Random seed (mandatory; MCMC is stochastic).}
}
\value{
\code{data.frame} (\code{patient_id}, \code{x}, \code{z}, \code{tau_hat},
\code{tau_lo}, \code{tau_hi} -- 95\% credible interval per person) with
attribute \code{ate} (posterior mean and SD of the average effect,
plus \code{lo}/\code{hi} posterior quantiles).
}
\description{
Like \code{\link[=fit_cate_grf]{fit_cate_grf()}}, but with the \emph{Bayesian Causal Forest}
(Hahn, Murray & Carvalho, 2020) -- the estimator with the
\strong{shorter leash}: the effect function is regularized separately
and strongly toward "everyone equal" ("shrink to homogeneity"),
and every person receives a full posterior distribution of their
effect (honest ranges instead of point values).
}
\details{
\strong{Honest limitation:} The bcf package knows no clusters -- the
therapist nesting is ignored here (unlike grf). Exactly this gap
is documented as an open methods point in the project; see
\code{\link[=fit_cate_bcf_ml]{fit_cate_bcf_ml()}} for the multilevel repair candidate.
}
