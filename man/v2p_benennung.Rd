% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/v2p_werkzeuge.R
\name{v2p_benennung}
\alias{v2p_benennung}
\title{Benennung post hoc auf einem v2P-Fit}
\usage{
v2p_benennung(fit, kandidaten = NULL, kandidaten_prod = NULL)
}
\arguments{
\item{fit}{Ergebnis von \code{\link[=fit_cate_dyade_v2p]{fit_cate_dyade_v2p()}} mit \code{halte_h = TRUE}.}

\item{kandidaten}{Benannte Liste von Funktionen \verb{function(x, c)}
(Form-Kandidaten), wie in \code{\link[=fit_cate_dyade_v2p]{fit_cate_dyade_v2p()}}.}

\item{kandidaten_prod}{Attribut-Produkt-Bibliothek
\code{list(patient = , therapeut = )}, wie in \code{\link[=fit_cate_dyade_v2p]{fit_cate_dyade_v2p()}}.}
}
\value{
Liste \code{beta} (Draws, Kandidaten x nsim), \code{p_gross}
(P(|beta| > .02), die auf statischen Produkt-Form-Welten
kalibrierte Benennungs-Schwelle; ausserhalb dieses Regimes eine
Uebertragung, keine Garantie -- die Benennungs-Schicht ist
exploratorisch), \code{hinweis}.
}
\description{
Rechnet die draw-weise Kandidaten-Regression der Benennungs-Schicht
NACH dem Fit -- etwa um mehrere Kandidaten-Bibliotheken (K = 5 vs.
K = 25) auf demselben Fit zu vergleichen, ohne den BCF neu zu
ziehen. Braucht die h-Draws: \code{\link[=fit_cate_dyade_v2p]{fit_cate_dyade_v2p()}} mit
\code{halte_h = TRUE} aufrufen.
}
\examples{
\donttest{
if (requireNamespace("stochtree", quietly = TRUE)) {
  s <- sim_stream(n_therapists = 8, patients_per_therapist = 6,
                  n_sessions = 2, z_level = "patient",
                  tau = 0.5, tau_xc = 0.35, seed = 5)
  b <- fit_cate_dyade_v2p(s, nburn = 50, nsim = 25, gitter_n = 12,
                          halte_h = TRUE, seed = 1)
  # zwei Bibliotheken auf DEMSELBEN Fit, ohne den BCF neu zu ziehen
  v2p_benennung(b, kandidaten = list(prod = function(x, c) x * c))$p_gross
  v2p_benennung(b, kandidaten = list(
    prod  = function(x, c) x * c,
    rausch = function(x, c) sin(3 * x) * c^2))$p_gross
}
}
}
