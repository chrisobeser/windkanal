% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_learners.R
\name{fit_cate_rlearner}
\alias{fit_cate_rlearner}
\title{Estimate personalized effects (R-learner, random forest)}
\usage{
fit_cate_rlearner(stream, num_trees = 500, B = 200, seed)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{num_trees}{Trees per forest (default 500).}

\item{B}{Bootstrap replicates for the ATE interval (default 200).}

\item{seed}{Random seed (mandatory).}
}
\value{
As \code{\link[=fit_cate_tlearner]{fit_cate_tlearner()}}.
}
\description{
Nie & Wager's R-learner (full text in quellen/): local centering
of the outcome (out-of-bag), then the treatment-residual-weighted
regression of the pseudo-outcome -- the orthogonalization idea
whose importance Dandl et al. and Tabib & Larocque independently
confirm. Propensity is \code{mean(z)} (randomized setting), so the
treatment residual is z - mean(z).
}
