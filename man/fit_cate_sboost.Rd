% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_learners.R
\name{fit_cate_sboost}
\alias{fit_cate_sboost}
\title{Estimate personalized effects (S-learner, gradient boosting)}
\usage{
fit_cate_sboost(stream, nrounds = 300, max_depth = 3, eta = 0.1, B = 200, seed)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{nrounds}{Boosting rounds (default 300).}

\item{max_depth}{Tree depth (default 3).}

\item{eta}{Learning rate (default 0.1).}

\item{B}{Bootstrap replicates for the ATE interval (default 200).}

\item{seed}{Random seed (mandatory).}
}
\value{
\code{data.frame} (\code{patient_id}, \code{x}, \code{z}, \code{tau_hat}) with
attribute \code{ate} (\code{estimate}, \code{se}, \code{lo}, \code{hi}; cluster
bootstrap).
}
\description{
The CatBoost/XGBoost class -- what applied matching studies
actually deploy: one boosted model of the outcome given features
plus treatment; the personal effect is the prediction difference
between z = 1 and z = 0. Field-typical defaults, deterministic
(\code{subsample = 1}, one thread). No shrinkage discipline toward
homogeneity, no native inference (therapist-cluster bootstrap).
}
