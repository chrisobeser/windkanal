% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_stan4bart.R
\name{fit_cate_stan4bart}
\alias{fit_cate_stan4bart}
\title{Estimate personalized effects (multilevel BART, stan4bart)}
\usage{
fit_cate_stan4bart(stream, chains = 2, warmup = 500, draws = 500, seed)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{chains, warmup, draws}{MCMC settings (defaults 2/500/500 per
chain, i.e. 1,000 posterior samples total).}

\item{seed}{Random seed (mandatory; MCMC is stochastic).}
}
\value{
As \code{\link[=fit_cate_bcf_ml]{fit_cate_bcf_ml()}}: \code{data.frame} with \code{tau_hat},
\code{tau_lo}, \code{tau_hi} and attribute \code{ate}.
}
\description{
Pruefling 14: the second multilevel implementation besides
\code{\link[=fit_cate_bcf_ml]{fit_cate_bcf_ml()}}, to test whether the random-intercept repair is
implementation-specific. stan4bart (Dorie et al., 2022, Entropy)
combines one BART surface with grouped terms estimated in Stan.
Model: \code{score_mean ~ bart(features + z) + (1 | therapist_id)}, with
stan4bart's native \code{treatment} support: the package generates the
counterfactual test frame itself, and individual effects are
per-draw contrasts \verb{(2 z - 1) (ev_observed - ev_counterfactual)}.
}
\details{
Difference to BCF by design: one response surface, no separate
effect forest, no propensity guard -- a genuine second
\emph{implementation family}, not a stochtree clone.

Note: dbarts is attached if necessary (stan4bart's formula
interface resolves \code{bart()} via dbarts).
}
