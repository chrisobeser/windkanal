% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_z.R
\name{fit_z}
\alias{fit_z}
\alias{fit_z_naive}
\alias{fit_z_satt}
\alias{fit_z_lmm}
\title{Treatment-Effekt-Schaetzer: naiv vs. genestet}
\usage{
fit_z_naive(snap)

fit_z_satt(snap)

fit_z_lmm(snap)
}
\arguments{
\item{snap}{Ein Snapshot (\code{data.frame} mit \code{score}, \code{z},
\code{therapist_id}, \code{patient_id}).}
}
\value{
Benannter Vektor \code{c(estimate, se)}.
}
\description{
Zwei Schaetzer fuer den Effekt des Cluster-Ebene-Treatments \code{z}
(Wahrheit: \code{tau} in \code{\link[=sim_stream]{sim_stream()}}). Hier beisst die Statistik:
}
\details{
\itemize{
\item \code{fit_z_naive()} -- \code{lm(score ~ z)}: behandelt jede Messung als
unabhaengig. Bei geclusterten Daten sind seine SEs fuer
Cluster-Ebene-Praediktoren \strong{zu klein} (anticonservativ) --
der Falsch-Positiv-Generator.
\item \code{fit_z_lmm()} -- \code{lmer(score ~ z + (1 | therapist_id) + (1 | patient_id))}: respektiert, dass \code{z} an der Therapeut:in
haengt und Information auf Cluster-Ebene begrenzt ist.
\item \code{fit_z_satt()} -- wie \code{fit_z_lmm()}, aber mit
\strong{Satterthwaite-Freiheitsgraden} (lmerTest): liefert zusaetzlich
einen echten p-Wert. Bei wenigen Clustern ist der Wald-z-Test
selbst des richtigen Modells noch zu liberal -- diese Variante
repariert das.
}

Interface-Konvention: \verb{fit_*}-Funktionen geben mindestens
\code{c(estimate, se)} zurueck; liefern sie zusaetzlich \code{p}, nutzen
\code{\link[=mc_run]{mc_run()}}, \code{\link[=run_peek]{run_peek()}} und \code{\link[=run_gate]{run_gate()}} diesen p-Wert statt der
Wald-Naeherung.

Gleiches Interface wie alle \verb{fit_*}-Funktionen.
}
