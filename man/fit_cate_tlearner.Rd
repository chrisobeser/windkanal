% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_learners.R
\name{fit_cate_tlearner}
\alias{fit_cate_tlearner}
\title{Estimate personalized effects (T-learner, random forest)}
\usage{
fit_cate_tlearner(stream, num_trees = 500, B = 200, seed)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{num_trees}{Trees per forest (default 500).}

\item{B}{Bootstrap replicates for the ATE interval (default 200).}

\item{seed}{Random seed (mandatory).}
}
\value{
\code{data.frame} (\code{patient_id}, \code{x}, \code{z}, \code{tau_hat}) with
attribute \code{ate} (\code{estimate}, \code{se}, \code{lo}, \code{hi}; cluster
bootstrap).
}
\description{
The class of tools the field is currently being taught (see the
meta-learner tutorial literature): fit one outcome forest on the
treated, one on the controls, and take the difference of their
predictions as the personal effect. No shrinkage discipline, no
native inference -- ATE uncertainty comes from a therapist-cluster
bootstrap (\code{\link[=cluster_boot_ate]{cluster_boot_ate()}}).
}
