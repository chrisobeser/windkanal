% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skala.R
\name{skala_begrenzen}
\alias{skala_begrenzen}
\title{Latenten Score auf eine begrenzte Fragebogen-Skala abbilden}
\usage{
skala_begrenzen(
  stream,
  minimum = 0,
  maximum = 27,
  schiefe = 0,
  mitte = 0,
  punkte_pro_einheit = (maximum - minimum)/6
)
}
\arguments{
\item{stream}{Datenstrom aus \code{\link[=sim_stream]{sim_stream()}} (oder ein
\code{data.frame} mit Spalte \code{score}).}

\item{minimum}{Untere Skalengrenze (ganzzahlig, Default 0).}

\item{maximum}{Obere Skalengrenze (ganzzahlig, Default 27 --
PHQ-9-Format).}

\item{schiefe}{Schiefe-Parameter des monotonen Tilts (Default 0 =
keine Schiefe). Geltungsgrenze: fuer \code{schiefe > 0} ist die
Abbildung nach unten hart beschraenkt
(\code{roh > (minimum + maximum)/2 - punkte_pro_einheit/schiefe});
die untere Skalengrenze wird strukturell unerreichbar, sobald
\code{punkte_pro_einheit <= schiefe * ((maximum - minimum)/2 - 0.5)}
-- spiegelbildlich fuer \code{schiefe < 0} die obere. \code{\link[=skala_werte]{skala_werte()}}
warnt in diesem Fall. Fuer Boden-/Deckeneffekte MIT Schiefe die
Verankerung \code{mitte} verschieben statt \code{schiefe} zu erhoehen.}

\item{mitte}{Latenter Wert, der auf die Skalenmitte abgebildet
wird (Default 0 -- die Streams sind um 0 zentriert).}

\item{punkte_pro_einheit}{Skalenpunkte je latenter Einheit vor
Zensierung (Default \code{(maximum - minimum) / 6}: +-3 latente
Einheiten spannen die Skala auf).}
}
\value{
Der Datenstrom mit zusaetzlicher ganzzahliger Spalte
\code{score_obs} in \verb{[minimum, maximum]} und Attribut \code{skala}
(Abbildungsparameter samt \code{estimand_default = "beobachtet"}).
Alle uebrigen Spalten, Attribute und die Klasse bleiben
unveraendert.
}
\description{
Bildet den stetigen (latenten) \code{score} eines Datenstroms auf eine
begrenzte, ganzzahlige Summen-Skala ab (BDI/PHQ-Stil): optionale
monotone Schiefe-Transformation, affine Verankerung, Zensierung an
Boden und Decke, Rundung auf ganze Punkte. Die latente Spalte
\code{score} bleibt unveraendert erhalten; die Schicht ist Opt-in --
ohne Aufruf aendert sich keine bestehende Welt.
}
\details{
Die beobachtete Skala ist der Default-Massstab fuer Audits auf
begrenzten Welten. Diese Schicht DEKLARIERT ihn (Attribut
\code{estimand_default}); die Verdrahtung der Schaetzer-Einstiege
(\verb{fit_*}, \code{\link[=mc_run]{mc_run()}}) auf \code{score_obs} ist ein eigener, noch
offener Posten -- bis dahin muessen Audits \code{score_obs} explizit
waehlen. Die latente Wahrheit wird stets mit ausgewiesen (siehe
\code{\link[=wahrheit_skala]{wahrheit_skala()}}).

Abbildung je Wert: \code{z = score - mitte}; bei \code{schiefe != 0} der
monotone Tilt \code{z = expm1(schiefe * z) / schiefe} (Grenzfall
\code{schiefe -> 0} ist die Identitaet; \code{schiefe > 0} streckt die obere
und staucht die untere Flanke, auf der Skala also Rechtsschiefe);
dann \code{roh = (minimum + maximum)/2 + punkte_pro_einheit * z},
zensiert auf \verb{[minimum, maximum]}, gerundet auf ganze Punkte.
}
\examples{
s <- sim_stream(n_therapists = 10, patients_per_therapist = 5,
                n_sessions = 3, tau = 1, seed = 42)
b <- skala_begrenzen(s, minimum = 0, maximum = 27)
range(b$score_obs)
# Deckeneffekt: hohe Verankerung sammelt Masse am Maximum
d <- skala_begrenzen(s, minimum = 0, maximum = 27, mitte = -3)
mean(d$score_obs == 27)
}
\seealso{
\code{\link[=skala_werte]{skala_werte()}} fuer die reine Abbildung,
\code{\link[=wahrheit_skala]{wahrheit_skala()}} fuer die Wahrheit auf beiden Ebenen.
}
