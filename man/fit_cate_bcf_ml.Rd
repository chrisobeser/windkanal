% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_bcf_ml.R
\name{fit_cate_bcf_ml}
\alias{fit_cate_bcf_ml}
\title{Estimate personalized effects (multilevel BCF, stochtree)}
\usage{
fit_cate_bcf_ml(
  stream,
  nburn = 500,
  nsim = 500,
  pihat = c("constant", "glm"),
  rfx = TRUE,
  seed
)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{nburn, nsim}{MCMC burn-in and draws (defaults 500/500).}

\item{pihat}{Propensity estimate as in \code{\link[=fit_cate_bcf]{fit_cate_bcf()}}:
\code{"constant"} (default) or \code{"glm"}.}

\item{rfx}{Include therapist random intercepts (\code{TRUE}, default --
the multilevel repair). \code{FALSE} runs stochtree's BCF \emph{without}
random effects: the fast standard-BCF doppelganger, used to
certify implementation equivalence with the old bcf package.}

\item{seed}{Random seed (mandatory; MCMC is stochastic).}
}
\value{
\code{data.frame} (\code{patient_id}, \code{x}, \code{z}, \code{tau_hat},
\code{tau_lo}, \code{tau_hi}) with attribute \code{ate} (\code{estimate}, \code{se},
\code{lo}/\code{hi} = posterior quantiles).
}
\description{
Like \code{\link[=fit_cate_bcf]{fit_cate_bcf()}}, but with the \strong{multilevel BCF} from the
stochtree package by the BCF authors (Herren, Hahn et al.): the
therapist nesting enters the model as a \strong{random intercept}
(\code{rfx_group_ids}) -- the fix, published 2025/26, for exactly the
weakness we measured in standard bcf (coverage collapse under
cluster-level treatments).
}
\details{
Side finding of the integration: stochtree is ~30x faster than
the old bcf package and seed-deterministic (no floating-point
noise).

\strong{Documented preset:} stochtree additionally runs \code{num_gfr = 5}
grow-from-root warm-start iterations (the authors' package
default); we adopt it deliberately and document it here instead
of letting it run along silently.
}
