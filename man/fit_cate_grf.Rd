% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate.R
\name{fit_cate_grf}
\alias{fit_cate_grf}
\title{Estimate personalized effects (causal forest)}
\usage{
fit_cate_grf(stream, num_trees = 1000, honesty = TRUE, clusters = TRUE, seed)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{num_trees}{Number of trees (default 1000).}

\item{honesty}{Honest splitting (grf's hallmark: one half of the
data grows the trees, the other estimates the values -- buys
honest intervals, costs effective sample size). \code{FALSE}
disables it. Default \code{TRUE} (= grf default).}

\item{clusters}{Respect therapists as clusters (cluster-robust
inference). \code{FALSE} = ignore nesting -- this is how studies
without cluster correction compute. Default \code{TRUE}.}

\item{seed}{Random seed for the forest (mandatory).}
}
\value{
\code{data.frame} (\code{patient_id}, \code{x}, \code{z}, \code{tau_hat}) with
attributes \code{ate} (average effect with SE, cluster-robust) and
\code{num_trees}.
}
\description{
Estimates the treatment effect (CATE) for \strong{every person} with
\code{grf::causal_forest} -- the "cautious pattern hunter": it may
find heterogeneity, but is regularized against inventing
patterns. Nesting is respected: therapists enter the estimation
as clusters (cluster-robust inference).
}
\details{
The outcome is the mean score per person (\code{\link[=patients]{patients()}}); the
covariate is \code{x}. Truth in the simulator: individual effect
\code{tau + tau_x * x} (with \code{reliability_x < 1} operating on \emph{true}
x -- the estimate on measured x is attenuated accordingly).
}
