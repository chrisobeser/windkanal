% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_run.R
\name{mc_run}
\alias{mc_run}
\title{Monte-Carlo-Experiment deklarieren und ausfuehren}
\usage{
mc_run(n_reps, sim_args, fit_fns, seed_start = 1, alpha = 0.05)
}
\arguments{
\item{n_reps}{Anzahl simulierter Welten.}

\item{sim_args}{Benannte Liste von Argumenten fuer \code{\link[=sim_stream]{sim_stream()}}
(ohne \code{seed} -- die Seeds vergibt \code{mc_run}).}

\item{fit_fns}{Benannte Liste von \verb{fit_*}-Funktionen.}

\item{seed_start}{Erster Seed; Welt i nutzt \code{seed_start + i - 1}.}

\item{alpha}{Signifikanzniveau fuer die \code{significant}-Spalte.}
}
\value{
\code{data.frame}: \code{rep}, \code{seed}, \code{estimator}, \code{estimate},
\code{se}, \code{significant}.
}
\description{
Wiederholt eine Simulation ueber viele Welten (Seeds) und wendet
auf jede Welt einen oder mehrere Schaetzer an. Das Experiment ist
damit vollstaendig durch seine Argumente beschrieben -- Windkanal-
Regel: ein Ergebnis, das man nicht aus \code{mc_run()}-Argumenten
reproduzieren kann, existiert nicht.
}
\examples{
res <- mc_run(n_reps = 20,
              sim_args = list(n_therapists = 10,
                              patients_per_therapist = 5,
                              n_sessions = 4, icc = 0.2, tau = 0),
              fit_fns = list(naiv = fit_z_naive))
mc_summary(res, truth = 0)

}
