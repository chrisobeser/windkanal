% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dyade_h_wahr.R
\name{dyade_h_wahr}
\alias{dyade_h_wahr}
\title{Wahre h-Komponente einer Kongruenz-Form (Doppelzentrierung)}
\usage{
dyade_h_wahr(
  x,
  c,
  form = c("product", "misfit_quad", "similarity_abs", "fenster"),
  fenster_delta = 0.5,
  tau_xc = 1,
  therapist_of = NULL
)
}
\arguments{
\item{x}{Patientenwerte (Vektor; die Gitter-Zeilen).}

\item{c}{Therapeutenwerte (Vektor, ein Wert je Therapeut:in;
die Gitter-Spalten).}

\item{form, fenster_delta}{Wie in \code{\link[=dyade_form]{dyade_form()}}.}

\item{tau_xc}{Skalierung (Default 1).}

\item{therapist_of}{Optional: Index des Therapeuten je Patient
(Laenge wie \code{x}, Werte in \code{1:length(c)}); dann enthaelt das
Ergebnis zusaetzlich \code{realisiert}, das wahre h der realisierten
Paare -- die Referenz fuer personenbezogene Metriken.}
}
\value{
Liste: \code{h} (Matrix length(x) x length(c)), \code{f}, \code{g},
\code{tau0}, optional \code{realisiert}.
}
\description{
Die Wahrheits-Referenz fuer dyadische Experimente: Die FORM selbst
(etwa \code{-(x-c)^2}) enthaelt Haupteffekt-Anteile -- bei der
quadratischen Kongruenz zur Haelfte. Matching-Kennzahlen muessen
daher gegen die h-KOMPONENTE gerechnet werden, nie gegen die rohe
Form. Diese Funktion berechnet sie numerisch:
\code{tau_xc * dyade_form(...)} am Produktgitter der uebergebenen
Raender, doppelzentriert.
}
\examples{
set.seed(2)
x <- rnorm(60); c <- rnorm(8)
w <- dyade_h_wahr(x, c, "misfit_quad", tau_xc = 0.35,
                  therapist_of = rep(1:8, length.out = 60))
max(abs(rowMeans(w$h)))   # doppelzentriert: ~0
length(w$realisiert)
}
