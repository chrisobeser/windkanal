% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_learners.R
\name{fit_cate_mob}
\alias{fit_cate_mob}
\title{Estimate personalized effects (model-based forest, MOB)}
\usage{
fit_cate_mob(stream, num_trees = 200, B = 200, seed)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{num_trees}{Trees in the personalized-model forest
(default 200 -- pmforest is costlier per tree than ranger).}

\item{B}{Bootstrap replicates for the ATE interval (default 200).}

\item{seed}{Random seed (mandatory).}
}
\description{
The second tree philosophy (Zeileis/Hothorn/Seibold tradition;
the hybrid family that performed best overall in Tabib &
Larocque and anchors Dandl et al., full texts in quellen/):
instead of splitting on effect heterogeneity directly, each leaf
carries a \emph{model} (here \code{lm(score_mean ~ z)}), and the forest of
personalized models yields a per-person treatment coefficient
(model4you::pmforest / pmodel). Bootstrap inference as in the
other learners.
}
