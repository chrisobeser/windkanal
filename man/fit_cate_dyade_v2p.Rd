% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_dyade_v2p.R
\name{fit_cate_dyade_v2p}
\alias{fit_cate_dyade_v2p}
\title{Dyadischer BCF v2P: Flaeche plus draw-weise Zerlegungs-Projektion}
\usage{
fit_cate_dyade_v2p(
  stream,
  nburn = 500,
  nsim = 500,
  pihat = c("mean", "glm"),
  gitter = c("quantil", "empirisch"),
  gitter_n = 30,
  kandidaten = NULL,
  kandidaten_prod = NULL,
  halte_h = FALSE,
  rfx_slope = FALSE,
  seed
)
}
\arguments{
\item{stream}{Datenstrom aus \code{\link[=sim_stream]{sim_stream()}} (braucht therapist_c).}

\item{nburn, nsim}{MCMC burn-in / Draws (Default 500/500).}

\item{pihat}{\code{"mean"} (Default) oder \code{"glm"} -- dieselben zwei
Schaetzer wie in \code{\link[=fit_cate_bcf_dyade]{fit_cate_bcf_dyade()}}; die erste Option heisst
dort \code{"constant"}.}

\item{gitter}{Referenzgitter der Projektion: \code{"quantil"} (Default;
Quantilgitter \code{gitter_n} x \code{gitter_n} der beiden Raender) oder
\code{"empirisch"} (Produkt der empirischen Raender: ALLE beobachteten
x_i x ALLE Therapeuten-c_j). \code{"empirisch"} liefert zusaetzlich
\code{h_real} (h an den realisierten Paaren, draw-weise) und ist
Voraussetzung fuer \code{kandidaten_prod}.}

\item{gitter_n}{Quantilgitter-Punkte je Rand (Default 30; nur
\code{gitter = "quantil"}). Einzelne endliche \strong{ganze} Zahl >= 2: ein
gebrochener Wert setzt die Randpunkte auf `(1:gitter_n)/(gitter_n
\itemize{
\item 1)` und damit auf andere Quantile als gemeint.
}}

\item{kandidaten}{Optional: benannte Liste von Funktionen
\verb{function(x, c)} (Form-Kandidaten der Benennung, z. B.
\code{list(prod = function(x, c) x * c)}). Jeder Kandidat wird am
Gitter doppelzentriert und h draw-weise darauf regressiert;
Ergebnis im Attribut \code{benennung} (\code{beta}-Draws, \code{p_gross}).
Kandidaten mit identischer h-Komponente sind nach der
Doppelzentrierung exakt kollinear (dz(-(x-c)^2) = 2\emph{dz(x}c))
und brechen mit lautem Fehler ab.}

\item{kandidaten_prod}{Optional: Attribut-Produkt-Bibliothek
\verb{list(patient = Matrix n x A, therapeut = Matrix J x B)} (beide
mit colnames) -- alle A*B Produkte a_i * b_j werden am
empirischen Kreuz doppelzentriert und mitbenannt (Spaltenname
\code{prod_a_b}). Zeilen-Kontrakt: \code{patient} in der Reihenfolge von
\code{\link[=patients]{patients()}}, \code{therapeut} in Erstauftretens-Reihenfolge von
\code{therapist_id}. Braucht \code{gitter = "empirisch"}.}

\item{halte_h}{h-Draws behalten (\code{zerlegung$h_draws}, Matrix
Gitterpunkte x nsim) -- noetig fuer \code{\link[=v2p_benennung]{v2p_benennung()}} post hoc;
Default \code{FALSE} (Speicher).}

\item{rfx_slope}{Zusaetzlich zum Therapeuten-Random-Intercept einen
\strong{Random Slope auf z} mitschaetzen (rfx-Basis \code{cbind(1, z)}):
Therapeuten-Level-Heterogenitaet des Behandlungseffekts, die
nicht ueber das gemessene c laeuft, wird dann vom rfx-Term
aufgesogen statt in die (x, c)-Flaeche zu lecken. Default
\code{FALSE} = bit-identisch zum Verhalten ohne Slope. Gemessen in
einem Mikro-Experiment (16.07.): senkt die Fehlalarmrate der
Beziehungs-Schicht von .16 auf .10 bei vollem Power-Erhalt --
Teil-Mitigation, die Benennungs-Fehlalarme bleiben (Leckage
sitzt auch auf Patienten-Ebene).}

\item{seed}{Zufalls-Seed (Pflicht).}
}
\value{
\code{data.frame} wie \code{\link[=fit_cate_bcf_dyade]{fit_cate_bcf_dyade()}} (\code{tau_hat} etc.),
zusaetzlich Attribute \code{zerlegung} (Liste: \code{tau0} Draws, \code{sd_h}
Draws, \code{h_mean} Gittermatrix, \code{f_mean}, \code{g_mean}, \code{gitter},
\code{typ}; bei \code{gitter = "empirisch"} zusaetzlich \code{h_real_mean} und
\code{therapist_of}; mit \code{halte_h} zusaetzlich \code{h_draws}), \code{ate},
bei \code{gitter = "empirisch"} \code{h_real} (Matrix n x nsim, wahres
h-Pendant: \code{\link[=dyade_h_wahr]{dyade_h_wahr()}} mit \code{therapist_of}) und ggf.
\code{benennung}.
}
\description{
Fit identisch zu \code{\link[=fit_cate_bcf_dyade]{fit_cate_bcf_dyade()}} (explizites
Produkt-Feature gegen die BCF-tau-Schrumpfung), danach wird JEDER
Posterior-Draw der tau-Flaeche am Gitter der Raender
doppelzentriert in tau0 + f(x) + g(c) + h(x, c) zerlegt. Kein
Refit, keine Residuen: Die Zerlegung ist eine deterministische
Projektion des Draws, die Unsicherheit propagiert exakt. Das
Personen-Ranking kommt unveraendert aus der Gesamt-Flaeche:
\code{tau_hat} ist mit \code{\link[=fit_cate_bcf_dyade]{fit_cate_bcf_dyade()}} bei gleichem Seed
identisch.
}
\details{
Detektion ist AMPLITUDEN-basiert: Attribut \code{sd_h} traegt die
Posterior-Verteilung von SD_Gitter(h); Richtungs-/Korrelationsmasse
sind als Detektor wertlos (der Wald nutzt das Produkt-Feature auch
fuer Rauschen). Die Schwellen sind kalibriert (Detektion .07/.52,
Benennung .02/.77; siehe \code{\link[=v2p_entscheidung]{v2p_entscheidung()}}) -- die Schicht
selbst bleibt \strong{exploratorisch}, und die Kalibrierung gilt fuer
statische Produkt-Form-Welten, in anderen Regimen als
Uebertragung. Regel-Anwendung: \code{\link[=v2p_entscheidung]{v2p_entscheidung()}}; Benennung
post hoc: \code{\link[=v2p_benennung]{v2p_benennung()}}.

Das Referenzgitter variiert nur \verb{(x, c)}; alle uebrigen
Trainings-Features stehen auf 0. Das ist ihr Mittelwert nur, wenn
sie zentriert sind -- eine Eigenschaft der Datenquelle, nicht der
Funktion. Sind sie es nicht, warnt die Funktion mit der
Bedingungsklasse \code{windkanal_warnung_gitter_nullfeature} und nennt
die Spalten; die Zerlegung bleibt gueltig, die Flaeche liegt dann
nur an einem anderen Punkt dieser Features als im Stichprobenmittel.
Betroffen sind allein die \verb{x_noise*}-Spalten, die
\code{\link[=cate_features]{cate_features()}} neben \code{x} und \code{therapist_c} durchreicht. Aus
\code{\link[=sim_stream]{sim_stream()}} sind die per Konstruktion zentriert (\code{rnorm}), die
Wache zielt also auf Stroeme, deren Rausch-/Kovariatenspalten der
Aufrufer selbst gesetzt oder ueberschrieben hat (Plasmode, echte
Kovariaten).
}
\examples{
\donttest{
if (requireNamespace("stochtree", quietly = TRUE)) {
  s <- sim_stream(n_therapists = 8, patients_per_therapist = 6,
                  n_sessions = 2, z_level = "patient",
                  tau = 0.5, tau_xc = 0.35, seed = 5)
  b <- fit_cate_dyade_v2p(s, nburn = 50, nsim = 25, gitter_n = 12,
                          seed = 1)
  v2p_entscheidung(b)$p_sd
}
}
}
