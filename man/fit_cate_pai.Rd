% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_learners.R
\name{fit_cate_pai}
\alias{fit_cate_pai}
\title{Estimate personalized effects (PAI: linear T-learner)}
\usage{
fit_cate_pai(stream, B = 200, seed)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{B}{Bootstrap replicates for the ATE interval (default 200).}

\item{seed}{Random seed (mandatory).}
}
\value{
As \code{\link[=fit_cate_tlearner]{fit_cate_tlearner()}}.
}
\description{
The field's own historical method -- the Personalized Advantage
Index tradition (DeRubeis school): one \emph{linear} regression per
arm, and the difference of their predictions is the personal
effect. Implemented in its simplest field-typical form (all
candidate features as main effects, no variable selection --
selection variants can be added later). Deterministic given the
data; the seed feeds the bootstrap (\code{\link[=cluster_boot_ate]{cluster_boot_ate()}}).
}
