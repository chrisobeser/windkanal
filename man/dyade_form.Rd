% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dyade_form.R
\name{dyade_form}
\alias{dyade_form}
\title{Kongruenz-Formen der dyadischen Passung (zentriert, normiert)}
\usage{
dyade_form(
  x,
  c,
  form = c("product", "misfit_quad", "similarity_abs", "fenster"),
  fenster_delta = 0.5
)
}
\arguments{
\item{x}{Patientenmerkmal (numerisch, Standardnormal-Skala).}

\item{c}{Therapeutenmerkmal derselben Zeilen (numerisch).}

\item{form}{\code{"product"} (Default), \code{"misfit_quad"},
\code{"similarity_abs"} oder \code{"fenster"}.}

\item{fenster_delta}{Fensterbreite delta (nur \code{form = "fenster"};
Default 0.5). Eine \strong{einzelne} endliche Zahl -- ein Vektor wuerde
still gegen \code{x} und \code{c} recyceln. Zulaessig ist (0, 3]: jenseits
von delta = 3 faengt
das Fenster fast jedes Paar (p > .96) und die SD-Normierung
teilt durch nahe null -- die Form degeneriert zur Konstanten.}
}
\value{
Numerischer Vektor: zentrierter, normierter Formwert; die
Beitrags-Formel im Generator ist \code{tau_xc * dyade_form(...)}.
}
\description{
Die Formen-Familie des dyadischen Terms: Wie wirkt die Paarung
(x, c) -- als Produkt, als quadratische Kongruenz, als
Betrags-Aehnlichkeit oder als Passungs-Zone (Fenster)? Jede Form
wird \strong{zentriert} (Erwartungswert 0 unter unabhaengigen
Standardnormal-Raendern, damit \code{tau} nicht stillschweigend
verschoben wird) und \strong{normiert} (SD 1 unter demselben
Referenzmass, damit \code{tau_xc} ueber Formen vergleichbar bleibt).
}
\details{
Analytische Konstanten (X, C ~ N(0,1) unabhaengig; D = X - C ~ N(0, 2)).
E und SD sind exakt; h-Anteile und Produktnaehe haengen vom
Referenzmass ab (Populationswert vs. Quantilgitter) und sind darum
als Spannen angegeben:
\itemize{
\item \code{misfit_quad} -(x-c)^2: E = -2, SD = sqrt(8); der h-Anteil ist
EXAKT 2xc (Produktform) -- 50 \% der Varianz sind Haupteffekte.
\item \code{similarity_abs} -|x-c|: E = -2/sqrt(pi), SD = sqrt(2 - 4/pi);
h-Anteil ~.55-.60, Produktnaehe Kor(h, xc) ~ .89-.91.
\item \code{fenster} \verb{1\{|x-c| <= delta\}}: E = p = 2*Phi(delta/sqrt(2)) - 1,
SD = sqrt(p(1-p)); h-Anteil ~.90, Produktnaehe ~ .31-.35 --
der Stresstest der Familie.
}
}
\examples{
set.seed(1)
x <- rnorm(1e5); c <- rnorm(1e5)
# alle Formen sind zentriert und auf SD 1 normiert (n gross genug,
# damit der SD-Schaetzer des Produkts das auch zeigt)
sapply(c("product", "misfit_quad", "similarity_abs", "fenster"),
       function(f) sd(dyade_form(x, c, f)))
}
