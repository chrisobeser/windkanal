% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plasmode_world.R
\name{plasmode_world}
\alias{plasmode_world}
\title{Plasmode-Welt: echte Kovariaten und Therapeuten-Struktur,
injizierte bekannte Wahrheit}
\usage{
plasmode_world(
  kohorte,
  x_spalte,
  n_sessions = 4,
  icc = 0.1,
  tau = 0.5,
  tau_x = 0,
  tau_x_form = c("linear", "step", "quadratic"),
  z_level = c("patient", "therapist"),
  seed
)
}
\arguments{
\item{kohorte}{data.frame mit mindestens \code{patient_id},
\code{therapist_id} und der in \code{x_spalte} benannten Kovariate
(z. B. TONI: \code{t0_ace_summe}). Zeilen mit NA in x fliegen mit
Warnung heraus (dokumentierte Politik E-1: complete case je
Analyse).}

\item{x_spalte}{Name der Kovariate, die als Moderator x dient;
wird z-standardisiert.}

\item{n_sessions, icc, tau, tau_x, tau_x_form, seed}{wie in
\code{\link[=sim_stream]{sim_stream()}}; die Wahrheit ist tau + tau_x * h(x).}

\item{z_level}{"patient" (Dyaden-Achse, Default) oder "therapist".}
}
\value{
data.frame mit den sim_stream-Spalten (\code{patient_id},
\code{therapist_id}, \code{session}, \code{z}, \code{x}, \code{score}), Attribut
\code{wahrheit} (Vektor der wahren Individual-Effekte) und
\code{plasmode_info}.
}
\description{
Baut einen sim_stream-kompatiblen Datenstrom aus einer ECHTEN
Kohorten-Tabelle (z. B. TONI/PsyTOM): echte Patient:innen-Merkmale
(x), echte Therapeuten-Zuordnung samt realer Caseload-Verteilung --
aber Treatment, Effekt und Outcome werden nach der windkanal-
Gleichung mit BEKANNTER Wahrheit erzeugt. So prueft man Schaetzer
unter echter Kovariaten-Verteilung (Schiefe!) und echter
Cluster-Struktur, ohne die Wahrheit zu verlieren (Kovariaten-
Plasmode; Rezept-Prinzip: die Kohorte selbst bleibt lokal und
wird nie mitverteilt).
}
