% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replay.R
\name{replay}
\alias{replay}
\title{Replay: den Datenstrom in Zeitreihenfolge nachspielen}
\usage{
replay(stream, at_weeks, fit_fn = fit_slope, min_rows = 10)
}
\arguments{
\item{stream}{Datenstrom aus \code{\link[=sim_stream]{sim_stream()}}.}

\item{at_weeks}{Vektor von Kalenderwochen, zu denen ein Snapshot
gezogen und gefittet wird.}

\item{fit_fn}{Funktion, die einen Snapshot (\code{data.frame}) nimmt und
ein benanntes Vektor-/Listenpaar \code{c(estimate = ..., se = ...)}
zurueckgibt. Default: \code{\link[=fit_slope]{fit_slope()}} (naives lm, siehe dort).}

\item{min_rows}{Snapshots mit weniger Zeilen werden uebersprungen
(Schaetzung = NA), Default 10.}
}
\value{
\code{data.frame} mit einer Zeile pro Snapshot: \code{snapshot_week},
\code{n_obs}, \code{n_patients}, \code{n_therapists}, \code{estimate}, \code{se},
\code{ci_low}, \code{ci_high} (95\%-Wald-Intervall).
}
\description{
Fittet dasselbe Modell auf eine Folge von Snapshots und sammelt die
Schaetzungen ein -- so laesst sich zusehen, wie sich ein Ergebnis
mit wachsendem Datenstrom schaerft (oder wandert). Das ist die
Grundfigur des lebenden Systems und spaeter die Basis von
Peeking-Simulation und Release-Gates.
}
\examples{
s <- sim_stream(n_therapists = 10, patients_per_therapist = 10,
                n_sessions = 8, seed = 42)
replay(s, at_weeks = c(10, 20, 30, 40, 50, 60))

}
