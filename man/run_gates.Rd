% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_gates.R
\name{run_gates}
\alias{run_gates}
\title{Mehrere Release-Gates mit verteiltem Irrtums-Budget}
\usage{
run_gates(
  stream,
  fit_fn,
  at_weeks,
  alpha = 0.05,
  correction = c("bonferroni", "none"),
  min_rows = 30
)
}
\arguments{
\item{stream}{Datenstrom aus \code{\link[=sim_stream]{sim_stream()}}.}

\item{fit_fn}{Eine \verb{fit_*}-Funktion.}

\item{at_weeks}{Vorab festgelegte Gate-Termine (Kalenderwochen).}

\item{alpha}{Gesamt-Irrtumsbudget.}

\item{correction}{\code{"bonferroni"} (Default) oder \code{"none"}.}

\item{min_rows}{Snapshots mit weniger Zeilen werden uebersprungen.}
}
\value{
Liste mit \code{gates} (data.frame: \code{week}, \code{estimate}, \code{se},
\code{alpha_local}, \code{significant}), \code{hit} und \code{first_hit_week}.
}
\description{
Realistischer als ein Einzeltermin (\code{\link[=run_gate]{run_gate()}}): das System darf
zu mehreren \strong{vorab festgelegten} Terminen pruefen. Damit das
Gesamt-Irrtumsrisiko bei \code{alpha} bleibt, wird das Budget auf die
Termine verteilt:
}
\details{
\itemize{
\item \code{correction = "bonferroni"}: jeder Termin prueft mit \code{alpha / k}
(einfach, garantiert gueltig, etwas konservativ).
\item \code{correction = "none"}: keine Verteilung -- absichtlich falsch,
um zu zeigen, was unkorrigiertes Mehrfach-Pruefen kostet.
}

Feinere Verteilungen (O'Brien-Fleming/Pocock-Alpha-Spending, wie in
klinischen Studien ueblich) sind als spaetere Option vorgesehen.
}
