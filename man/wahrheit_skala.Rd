% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skala.R
\name{wahrheit_skala}
\alias{wahrheit_skala}
\title{Wahrheit auf beiden Ebenen: latenter und beobachteter Effekt}
\usage{
wahrheit_skala(
  ...,
  minimum = 0,
  maximum = 27,
  schiefe = 0,
  mitte = 0,
  punkte_pro_einheit = (maximum - minimum)/6,
  session = NULL,
  seed
)
}
\arguments{
\item{...}{Weltparameter fuer \code{\link[=sim_stream]{sim_stream()}} (ohne \code{dropout},
\code{z_force}, \code{seed} -- diese setzt die Funktion selbst).}

\item{minimum}{Untere Skalengrenze (ganzzahlig, Default 0).}

\item{maximum}{Obere Skalengrenze (ganzzahlig, Default 27 --
PHQ-9-Format).}

\item{schiefe}{Schiefe-Parameter des monotonen Tilts (Default 0 =
keine Schiefe). Geltungsgrenze: fuer \code{schiefe > 0} ist die
Abbildung nach unten hart beschraenkt
(\code{roh > (minimum + maximum)/2 - punkte_pro_einheit/schiefe});
die untere Skalengrenze wird strukturell unerreichbar, sobald
\code{punkte_pro_einheit <= schiefe * ((maximum - minimum)/2 - 0.5)}
-- spiegelbildlich fuer \code{schiefe < 0} die obere. \code{\link[=skala_werte]{skala_werte()}}
warnt in diesem Fall. Fuer Boden-/Deckeneffekte MIT Schiefe die
Verankerung \code{mitte} verschieben statt \code{schiefe} zu erhoehen.}

\item{mitte}{Latenter Wert, der auf die Skalenmitte abgebildet
wird (Default 0 -- die Streams sind um 0 zentriert).}

\item{punkte_pro_einheit}{Skalenpunkte je latenter Einheit vor
Zensierung (Default \code{(maximum - minimum) / 6}: +-3 latente
Einheiten spannen die Skala auf).}

\item{session}{Sitzung, fuer die die Wahrheit berechnet wird
(Default: letzte Sitzung des Stroms).}

\item{seed}{Zufalls-Seed des kontrafaktischen Doppellaufs
(Pflicht).}
}
\value{
Liste: \code{session}; \code{ate_latent} und \code{ate_beobachtet}
(mittlere Effekte je Skala); \code{effekte_x_terzile} (\code{data.frame}
der bedingten Effekte je x-Terzil auf beiden Skalen);
\code{spann_latent} (Spannweite der Terzil-Effekte in \strong{latenten
Einheiten}) und \code{spann_beobachtet} (dieselbe Spannweite in
\strong{Skalenpunkten}); \code{schein_heterogenitaet}; \code{skala} (Parameter).

⚠ \code{spann_latent} und \code{spann_beobachtet} stehen in
\strong{verschiedenen Einheiten} -- zwischen ihnen liegt der Faktor
\code{punkte_pro_einheit} (Default \code{(maximum - minimum) / 6}). Ihre
\strong{Differenz ist keine Kennzahl}, sondern ueberwiegend die
Umrechnungskonstante: in einer gemessenen Welt meldet sie
\code{+4.33}, waehrend die begrenzte Skala die Heterogenitaet dort in
Wahrheit leicht \strong{gestaucht} hat (\code{-0.09}) -- Vorzeichen und
Groessenordnung beide falsch. Die Schein-Heterogenitaet der Skala
ist die einheitenbereinigte Differenz
\code{spann_beobachtet / punkte_pro_einheit - spann_latent}, also der
Vergleich beider Spannweiten auf der \strong{latenten} Skala; sie
faehrt als \code{schein_heterogenitaet} im Rueckgabewert mit (in
Skalenpunkten: mal \code{punkte_pro_einheit}). Positiv = die begrenzte
Skala blaeht Heterogenitaet auf, negativ = sie staucht sie.
⚠ Die Umrechnung \verb{/ punkte_pro_einheit} ist die \strong{lineare}
Ruecknahme der Skalen-Abbildung; unter aktivem \code{schiefe}-Tilt ist
die Abbildung nicht linear, die Kennzahl ist dann eine
Naeherung. Der zweite, von der Umrechnung unabhaengige Weg ist
der Vergleich von \code{spann_beobachtet} gegen einen zweiten Lauf mit
unerreichbaren Grenzen bei \strong{gleichem} \code{punkte_pro_einheit}.
}
\description{
Berechnet fuer eine deklarierte Welt die wahren Behandlungseffekte
auf der latenten UND der begrenzten beobachteten Skala, per
kontrafaktischem Doppellauf: \code{\link[=sim_stream]{sim_stream()}} wird zweimal mit
identischem Seed gezogen (\code{z_force = 0} bzw. \code{1}); die Zuweisung
wird dabei normal gezogen (identischer RNG-Verbrauch) und erst
danach ueberschrieben, sodass beide Laeufe bis auf \code{z} identisch
sind (CRN) und je Zeile beide Potential Outcomes vorliegen. Drei
Wachen pruefen den Gleichlauf (\code{patient_id}, \code{session}, \code{x}
identisch), eine vierte den RNG-Endzustand beider Laeufe auf
z-abhaengigen Zufallszahlen-Verbrauch; schlaegt eine an, stoppt
der Lauf.
}
\details{
Die Wahrheit ist als Populations-Estimand der DROPOUT-FREIEN Welt
definiert: \code{dropout = 0} wird intern gesetzt.
\code{dropout_informative} ist unter erzwungenem \code{dropout = 0}
wirkungslos (Warnung). Messfehler (\code{reliability_score < 1}) bleibt
Teil des beobachteten Estimands -- die Zensierung wirkt auf den
GEMESSENEN Wert --; er kuerzt sich im Doppellauf nicht heraus und
erhoeht die MC-Restunschaerfe der Wahrheitsrechnung. \code{p_treated}
und \code{confounding} duerfen als Weltparameter durchgereicht werden
-- sie formen nur die verworfene Zuweisungs-Ziehung und aendern
die Potential Outcomes nicht. Die x-Terzile werden auf dem
Strom-\code{x} gebildet, bei \code{reliability_x < 1} also auf dem
fehlerbehafteten x (dasselbe x, das ein Schaetzer saehe). Nur
\code{z_type = "binary"} wird unterstuetzt.
}
\examples{
w <- wahrheit_skala(n_therapists = 20, patients_per_therapist = 10,
                    n_sessions = 3, z_level = "patient",
                    tau = 0.5, tau_x = 0.6, seed = 4242)
w$ate_latent
w$ate_beobachtet
w$effekte_x_terzile
# NICHT spann_beobachtet - spann_latent (verschiedene Einheiten):
w$schein_heterogenitaet
}
