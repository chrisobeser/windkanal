% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate.R
\name{patients}
\alias{patients}
\title{Condense the data stream to the person level}
\usage{
patients(
  stream,
  outcome = c("mean", "at_session", "slope"),
  session = max(stream$session)
)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{outcome}{\code{"mean"} (default), \code{"at_session"}, or \code{"slope"}.}

\item{session}{Fixed session for \code{outcome = "at_session"}
(default: last planned session in the stream).}
}
\value{
\code{data.frame}: \code{patient_id}, \code{therapist_id}, \code{z}, \code{x},
\code{n_obs}, \code{score_mean} (or \code{score_at}/\code{score_slope}).
}
\description{
Many questions live at the person level, not the session level.
This function condenses the stream: one row per patient with
treatment membership, feature \code{x}, number of observed sessions,
and mean score.
}
\details{
\strong{Estimand warning:} Under informative dropout, the mean over
\emph{observed} sessions is itself selection-biased (whoever stays
longer contributes more and better values). Alternatives:
\code{outcome = "at_session"} (score at a fixed session, only persons
observed there) or \code{outcome = "slope"} (personal trajectory
slope).
}
