% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cate_merf.R
\name{fit_cate_merf}
\alias{fit_cate_merf}
\title{Estimate personalized effects (mixed-effects random forest, MERF)}
\usage{
fit_cate_merf(stream, ntree = 300, iter = 20, B = 200, seed)
}
\arguments{
\item{stream}{Data stream from \code{\link[=sim_stream]{sim_stream()}}.}

\item{ntree}{Baeume des Forests (default 300).}

\item{iter}{Maximale EM-Iterationen (default 20, LongituRF-nah).}

\item{B}{Bootstrap replicates for the ATE interval (default 200;
therapist-cluster bootstrap as for the other learners).}

\item{seed}{Random seed (mandatory).}
}
\value{
\code{data.frame} (\code{patient_id}, \code{x}, \code{z}, \code{tau_hat}) with
attribute \code{ate} (\code{estimate}, \code{se}, \code{lo}, \code{hi}; cluster
bootstrap).
}
\description{
Pruefling 16: the mixed-effects random forest (Hajjem et al., 2014,
J Stat Comput Simul; Implementierung: LongituRF, CRAN-Archiv 0.9) --
die MERF-Schiene der Li-et-al.-Familie. EM-artiger Wechsel zwischen
einem random forest fuer den festen Teil f(features, z) und BLUPs
fuer Therapeuten-Random-Intercepts. Individuelle Effekte sind
kontrafaktische Differenzen der Forest-Vorhersagen (S-Learner-Stil;
der Random Intercept kuerzt sich innerhalb des Therapeuten).
}
\details{
\code{sto = "none"} (kein stochastischer Prozess; wir arbeiten auf
Personen-Mitteln, nicht auf Verlaeufen); \code{time} ist dann nur ein
Pflicht-Platzhalter (Innerhalb-Cluster-Index).
}
