% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime.R
\name{regime}
\alias{regime}
\alias{run_peek}
\alias{run_gate}
\title{Analyse-Regime: Dauer-Peeking vs. Release-Gate}
\usage{
run_peek(stream, fit_fn, every = 4, alpha = 0.05, min_rows = 30)

run_gate(stream, fit_fn, at_week, alpha = 0.05, min_rows = 30)
}
\arguments{
\item{stream}{Datenstrom aus \code{\link[=sim_stream]{sim_stream()}}.}

\item{fit_fn}{Eine \verb{fit_*}-Funktion (z. B. \code{\link[=fit_z_lmm]{fit_z_lmm()}}).}

\item{every}{Peek-Intervall in Wochen (Default 4).}

\item{alpha}{Signifikanzniveau.}

\item{min_rows}{Snapshots mit weniger Zeilen werden uebersprungen.}

\item{at_week}{Gate-Termin (Kalenderwoche).}
}
\value{
\code{run_peek()}: Liste mit \code{looks} (data.frame aller Blicke:
\code{week}, \code{estimate}, \code{se}, \code{significant}), \code{hit} (wurde je
Signifikanz gemeldet?) und \code{first_hit_week}.
\code{run_gate()}: Liste mit \code{week}, \code{estimate}, \code{se}, \code{significant}.
}
\description{
Zwei Arten, denselben wachsenden Datenstrom zu befragen:
}
\details{
\itemize{
\item \code{run_peek()} -- der Dauer-Gucker: testet alle \code{every} Wochen und
"stoppt bei Signifikanz" (meldet den ersten Treffer). So arbeitet
ein lebendes System ohne Governance.
\item \code{run_gate()} -- das Release-Gate: \strong{ein} praeregistrierter Test
zu \strong{einem} vorab festgelegten Termin auf dem eingefrorenen
Snapshot. So arbeitet das System mit Governance (v0: Einzel-Gate;
Mehrfach-Gates mit Alpha-Spending folgen).
}

Beide nutzen dieselben Snapshots und denselben Schaetzer -- der
Unterschied ist reines Protokoll. Genau das soll messbar werden.
}
